# Minimal File Concatenator

A simple, browser-based tool to concatenate multiple files.

## Usage
Simply open `index.html` in any modern web browser. Drag and drop files or folders onto the page, select the files you want to include, and click "Copy Concatenated" to copy their combined contents to your clipboard.
